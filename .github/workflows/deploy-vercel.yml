name: Deploy · Vercel

# Deploys the Sierra Estates web app to Vercel FROM GitHub (not Vercel's own
# git integration). Vercel's automatic git deploys are disabled via
# `git.deploymentEnabled: false` in vercel.json — this workflow is the single
# source of deploys.
#
# Required repo secret:
#   VERCEL_TOKEN  — a Vercel access token (Account Settings → Tokens)
# Project/org IDs are non-secret and already committed in .vercel/project.json.

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy target'
        default: 'production'
        type: choice
        options:
          - production
          - preview

concurrency:
  group: vercel-deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  VERCEL_ORG_ID: team_k2jaWfzeatcYG6Qpl0ooCxQh
  VERCEL_PROJECT_ID: prj_cJSEzNFE1JOAHzIdjrsJtVaqq7vJ

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # pnpm must be set up BEFORE setup-node so the cache key is correct
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Determine deploy target
        id: target
        run: |
          ENV="${{ github.event.inputs.environment || 'production' }}"
          echo "name=$ENV" >> "$GITHUB_OUTPUT"
          if [ "$ENV" = "production" ]; then
            echo "flag=--prod" >> "$GITHUB_OUTPUT"
          else
            echo "flag=" >> "$GITHUB_OUTPUT"
          fi

      - name: Normalize Vercel project settings (monorepo self-heal)
        run: |
          # Best-effort self-heal of the Vercel project settings. The target
          # project previously hosted the removed Vite admin-dashboard and
          # carried its overrides (buildCommand "pnpm --filter admin-dashboard
          # build", outputDirectory "dist"), which broke every deploy of the
          # Next.js app ("output directory dist not found"). Reset to Next.js
          # zero-config with the monorepo root directory. Non-2xx must NOT
          # abort the deploy — otherwise production never ships.
          code=$(curl -sS -o /dev/null -w '%{http_code}' -X PATCH \
            "https://api.vercel.com/v9/projects/${VERCEL_PROJECT_ID}?teamId=${VERCEL_ORG_ID}" \
            -H "Authorization: Bearer ${{ secrets.VERCEL_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"rootDirectory":"apps/sierra-estates-realty","framework":"nextjs","buildCommand":null,"installCommand":null,"outputDirectory":null,"devCommand":null}' || echo "000")
          if [ "$code" = "200" ] || [ "$code" = "204" ]; then
            echo "Project settings normalized: Next.js zero-config, root apps/sierra-estates-realty (HTTP $code)"
          else
            echo "::warning::Could not normalize Vercel project settings (HTTP $code). Continuing deploy — verify VERCEL_TOKEN has project-write scope and that the dashboard settings are framework=Next.js, root=apps/sierra-estates-realty, no build/install/output overrides."
          fi

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel project settings
        run: vercel pull --yes --environment=${{ steps.target.outputs.name }} --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build
        run: vercel build ${{ steps.target.outputs.flag }} --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy (prebuilt)
        run: vercel deploy --prebuilt ${{ steps.target.outputs.flag }} --token=${{ secrets.VERCEL_TOKEN }}
