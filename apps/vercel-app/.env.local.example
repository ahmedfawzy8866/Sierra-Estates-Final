# ============================================================
# Sierra Estates — Environment Variables
# Copy to .env.local and fill in values.
# NEVER commit .env.local to version control.
#
# This file is the canonical list of every variable the app reads
# (apps/sierra-estates-realty). Keep it in sync: a CI sweep of
# `process.env.*` vs this file is the source of truth.
# ============================================================

# ─── Firebase Client SDK (public; NEXT_PUBLIC_* ships to the browser) ───
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# ─── Firebase Admin SDK (server-side only; NEVER expose) ───
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
# Alternative to the 3 vars above: full service-account JSON as one string.
FIREBASE_SERVICE_ACCOUNT_JSON=

# ─── Internal auth / service secrets ───
# SBR_SECRET_KEY is the shared secret checked by lib/server/auth-guard.ts
# (X-SBR-SECRET-KEY header / Bearer) for service + webhook callers.
SBR_SECRET_KEY=
CRON_SECRET=

# ─── AI / Gemini ───
GOOGLE_AI_API_KEY=
GOOGLE_GENAI_API_KEY=
NEXT_PUBLIC_GEMINI_API_KEY=
# Selects the AI provider implementation in lib/ai.
AI_PROVIDER=
# Vertex AI (agents-core PropertyMatcherAgent — IAM/ADC auth, no API key).
# Requires GOOGLE_APPLICATION_CREDENTIALS or Workload Identity locally/in CI.
GOOGLE_CLOUD_LOCATION=europe-west1

# ─── WhatsApp — bulk outreach senders (4 dedicated numbers) ───
# Current pool: +201032206443, +201092048333, +201061399688, +201031622700.
# Digits only, no leading +. Consumed by lib/server/whatsapp-queue.ts for
# round-robin load balancing + per-number rate limiting.
WABA_NUMBER_1=
WABA_NUMBER_2=
WABA_NUMBER_3=
WABA_NUMBER_4=
# Meta WhatsApp Cloud API (inbound webhook + any Cloud-API sends)
WHATSAPP_API_TOKEN=
WHATSAPP_META_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=
# Branding suffix appended to outbound owner-outreach templates.
BRANDING_TAG=

# ─── WhatsApp — Twilio (outbound sending) ───
# lib/server/twilio-client.ts calls the Twilio REST API directly (no SDK).
# lib/server/whatsapp-queue.ts owns scheduling/rate-limits/retry on top of it.
# Each WABA_NUMBER_1-4 above must be a Twilio WhatsApp-approved sender on this account.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# Either a Messaging Service SID, or per-sender SIDs aligned with WABA_NUMBER_*.
TWILIO_MESSAGING_SERVICE_SID=

# ─── Telegram (high-priority alerts + webhook) ───
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_WEBHOOK_SECRET=

# Upstash Redis — shared by lib/server/rate-limit.ts (public endpoint rate
# limiting) and lib/server/whatsapp-queue.ts (WhatsApp outbound queue +
# per-number rate limiting). When BOTH vars are set, both use a Redis-backed
# store shared across all serverless instances. When unset, both fall back
# to a per-instance in-memory store — fine for local dev, NOT safe for a
# multi-instance Vercel deploy (rate limits/queue state won't be shared).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ─── Webhook Security ───
# Shared secret for authenticating incoming webhook requests.
# Must be at least 16 characters. Used by /api/webhooks/ endpoints.
WEBHOOK_SHARED_SECRET=

# n8n Workflow Engine
N8N_BASE_URL=http://localhost:5678
N8N_API_KEY=

# ─── Airtable (lead/listing ingestion) ───
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=

# ─── Google Sheets (ingestion + sync) ───
GOOGLE_SHEETS_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_KEY=
MASTER_SHEET_ID=
BROKER_INBOX_SHEET_ID=

# ─── Voice (Leila TTS) ───
ELEVENLABS_API_KEY=
LEILA_VOICE_ID=

# ─── Observability (Arize) ───
ARIZE_SPACE_ID=
ARIZE_API_KEY=
ARIZE_PROJECT_NAME=

# ─── Workflow / integration endpoints ───
N8N_BASE_URL=http://localhost:5678
N8N_API_KEY=
PYTHON_API_BASE_URL=http://localhost:8000
OPENCLAW_BASE_URL=
OPENCLAW_TOKEN=
ZAPIER_CALENDAR_WEBHOOK_URL=

# ─── Email (Gmail SMTP) ───
GMAIL_USER=
GMAIL_PASS=

# ─── Maps & reCAPTCHA (client) ───
NEXT_PUBLIC_MAPBOX_TOKEN=
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=

# ─── Firebase Admin App (API rewrites) ───
# URL of the Firebase Admin Cloud Functions app that handles webhook, cron,
# admin, sync, and agent routes. Next.js rewrites these paths to this URL.
# If unset, defaults to https://admin-api.sierra-estates.net
FIREBASE_ADMIN_URL=

# ─── App config / hosts ───
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_INTELLIGENCE_OS_URL=
# admin.sierra-estates.net — enables the admin host-split in middleware.ts.
ADMIN_HOST=
# Comma-separated origins allowed by CORS (lib/server/cors.ts).
ALLOWED_ORIGINS=
