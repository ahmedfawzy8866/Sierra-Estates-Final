{
  "name": "@sierra-estates/agents-core",
  "version": "1.0.0",
  "description": "Sierra Estates agent orchestration framework — 4 agents, 20+ role specs",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sierra-estates/exchange": "workspace:*",
    "@sierra-estates/memory-engine": "workspace:*",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.3"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}
