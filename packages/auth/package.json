{
  "name": "@sierra-estates/auth",
  "version": "2.0.0",
  "description": "Shared auth layer — ONE Firebase Auth with custom claims for both apps",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sierra-estates/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.7.2"
  }
}
