packages:
  - "apps/*"
  # Legacy/deprecated apps excluded from the workspace so they no longer
  # participate in `turbo run build/lint/type-check` (one pins TS ~6.0.2 and
  # reddened CI). Kept on disk for now; physical removal is a follow-up.
  - "!apps/admin-dashboard"
  - "!apps/sierra-blu-admin-portal"
  - "!apps/sierra-blu-realty"
  - "packages/*"
  - "functions"
